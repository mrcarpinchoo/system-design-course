/* global document */

// Module-specific JavaScript for this presentation.
// The shared presenter.js provides: applyTheme, toggleTheme, toggleLang,
// initParticles, animateCounter, triggerCounters, animateSlideElements,
// restartSVGAnimations, expandable card handling, and Reveal.js initialization.
//
// Override Reveal.js defaults by setting properties on window.PRES_CONFIG:
//   window.PRES_CONFIG = { width: 1280, height: 720, transition: 'fade' };
//
// Available defaults: width (960), height (600), margin (0.04), center (false),
// hash (true), transition ('slide'), transitionSpeed ('default'), controls (true),
// progress (true), slideNumber (true), overview (true), touch (true).

window.PRES_CONFIG = {};
